# name: ci

# on:
#   push:
#     branches: ["main", "master"]
#   pull_request:

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.10"

#       - name: Install dependencies
#         run: |
#           # Keep CI simple: install the project dependencies and test runner.
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Run smoke tests
#         env:
#           # Dummy values are enough because the smoke tests monkeypatch the
#           # external pipeline calls and do not contact real provider APIs.
#           GROQ_API_KEY: dummy
#           OPENAI_API_KEY: dummy
#           LLM_BACKENDS: groq
#         run: |
#           python -m pytest -q

name: Python Backend CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run Python compilation sanity check
      run: |
        # Compiles your python scripts to catch hidden SyntaxErrors or IndentationErrors
        python -m py_compile main.py